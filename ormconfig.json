{
  "type": "sqlite",
  "database": "./database.sqlite",
  "migrations": [
    "dist/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "dist/database/migrations"
  },
  "entities": [
    "dist/models/**/*.ts"
  ]
}
